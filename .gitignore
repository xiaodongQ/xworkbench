# 运行时产物
data/
*.db-journal
*.db
bin/
tmp-e2e-test/
config.json
.codegraph
# server 二进制（避免误忽略 cmd/server/static/ 等前端资源）
cmd/server/server
cmd/server/*.exe
# 本地编译产物
/server
/xworkbench
/xworkbench.exe
nohup.out
# Claude Code 本机配置（per-user 工具状态，不入库）
.claude/

# AI 任务烟雾测试产物（TwoSum/Sum 之类的 LeetCode 试写代码，曾经混进
# 源码目录污染 build。AI 任务 CWD 是仓库根，必须显式隔离防止再写入）
internal/sum/
internal/twosum/

# AI 沙盒（data/ai-sandbox/）—— 所有 AI 任务的 CWD 都在这里。
# 已被 data/ 涵盖，这里加显式条目做文档/防漏；claude/cbc 写出来的代码、
# 中间产物都落这里，永远不会被误 commit
data/ai-sandbox/
.playwright-cli
tasks/two-sum/
__pycache__/
